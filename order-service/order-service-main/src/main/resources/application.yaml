# Change order service running port
server:
  port: 8550

spring:
  application:
    name: order-service
  datasource:
    driver-class-name: org.postgresql.Driver
    username: spring_usr
    password: root
    url: jdbc:postgresql://localhost:8432/db_order
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true